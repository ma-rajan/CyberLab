# Server
PORT=3001
NODE_ENV=development

# SQLite
DATABASE_URL="file:./dev.db"

# Browser origin permitted to call the local API
CLIENT_ORIGIN=http://localhost:5173

# Session lifetime in days. Keep this value modest on shared computers.
SESSION_TTL_DAYS=7
